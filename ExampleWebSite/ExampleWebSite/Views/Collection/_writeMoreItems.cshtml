@model ExampleWebSite.ViewModels.CollectionDetailsViewModel

@foreach (var item in Model.items)
{
    <div class="col-md-3">
        <div class="card m-2 " style="width:90%;">
            <div class="card-body">
                <h5 class="card-title">@item.Title</h5>
                @if (Model.AvtorName == User.Identity.Name || User.IsInRole("admin"))
                {
                    <a class="btn" asp-action="Delete" asp-controller="Item" asp-route-id="@item.Id">@ViewResourcesModel["Delete"]</a>
                }
                <a class="btn" asp-action="Details" asp-controller="Item" asp-route-id="@item.Id">@ViewResourcesModel["Enter"]</a>
            </div>
        </div>
    </div>
}
